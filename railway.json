{
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm ci && npm run build && npx prisma generate --schema=prisma/schema.prisma"
  },
  "deploy": {
    "startCommand": "npx prisma migrate resolve --applied 20260525022902_init --schema=prisma/schema.prisma; npx prisma db push --schema=prisma/schema.prisma --accept-data-loss; node dist/src/main.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
